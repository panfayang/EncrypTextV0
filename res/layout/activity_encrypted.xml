<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/gradient"
    tools:context=".EncryptedActivity" >
    
    <TextView
        android:id="@+id/textViewEncrypted"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
       	android:layout_alignParentTop="true"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:layout_marginTop="40dp"
        android:maxLines="14"
        android:scrollbars="vertical"
        android:text="" />

    <Button
        android:id="@+id/copyEncryptedText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/textViewEncrypted"
        android:layout_marginBottom="52dp"
        android:text="@string/copy_text"
        android:onClick="copyText" />

    <Button
        android:id="@+id/copyKey"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/copyEncryptedText"
        android:layout_alignBottom="@+id/copyEncryptedText"
        android:layout_marginRight="23dp"
        android:layout_toLeftOf="@+id/copyEncryptedText"
        android:text="@string/copy_key"
        android:onClick="copyKey" />

</RelativeLayout>
